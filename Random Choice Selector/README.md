For this project simple enter your different choices separating each choice by a comma (,). 
Once you have input all your choices hit enter and the choice selector will randomly settle on one choice.
